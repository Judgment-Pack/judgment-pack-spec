# RFC 0002 — interim-review-regime record, round 3 (confirmation)

Reviewer: codex-cli 0.153.4, model gpt-6-astra (OpenAI), reasoning effort xhigh, read-only sandbox, run in a worktree of this repository at the reviewed commit, with read access to a snapshot of the reference runtime's `origin/main` (`d891b05`), to the experiments repository's Study 004 files, and to the files beside the worktree, among them the round-2 prompt, output and run log. It could write nothing; it rendered the RFC through this repository's site renderer in memory. It reports what it could and could not run or verify at the end of its output.
Different vendor than the model that drafted the change (Claude Fable 5.1, Anthropic), per RFC 0009 / GOVERNANCE.
Run: 2026-09-21, on commit `4335efc`. One run; none discarded. Verdict: **mergeable after fixes** — both round-2 findings confirmed resolved, no sentence of the RFC left inconsistent with its Scope section, and one new minor finding about rendering.

> Note: absolute local paths are normalized here as in rounds 1 and 2. The reviewer's links are written as plain `path:line` citations relative to this repository's root. The prompt's path to the runtime snapshot is written `<snapshots>/runtime`. The prompt and the reviewer's output are otherwise verbatim and complete.

## Prompt (verbatim)

```
You are the interim-review-regime peer reviewer for the judgment-pack-spec repository, per RFC 0009 / GOVERNANCE.md — a model from a different vendor than the one that drafted the change (Claude). This is ROUND 3, a confirmation round. Under review is the branch at commit 4335efc (`git log -3`), which amends rfcs/0002-judgment-graph.md and leaves it a `Draft`. Round 1 (on 2ac6baa) returned six findings and round 2 (on f76c519) confirmed them fixed and returned two more, one of them major; rfcs/reviews/0002-round-1.md and 0002-round-2.md hold the verbatim prompts, your findings and the maintainer's disposition of each, and 4335efc is the commit that applies round 2's (`git diff f76c519 4335efc`).

First verify each round-2 fix. Say per finding whether the edit resolves it, and whether it introduced a new inaccuracy:
- R2-1: in the Study 004 paragraph under Evidence, the clause about the outcome-as-input edge matching "the seam *between* systems" and the sentence calling the scope choice "still an open one" are struck, followed by a dated parenthetical note. Check that what is struck is exactly what the scope decision and the evidence overturn, that the within-policy observation before the strike is kept and is what the RFC's account of Study 004 supports, that the note's two statements are accurate (nothing was measured about the between-systems case because the frame could not contain it; the choice is decided under Scope), and that the paragraph still reads coherently into "The usual bounds apply". Check the Markdown: strikethrough spanning several lines, and the parenthetical's emphasis.
- R2-2: the inheritance sentence now reads that "the runtime's decisions on the reviewed-set lock, audit records and evaluation traces cover graphs too". Check against <snapshots>/runtime/docs/adr/0018-opt-in-evaluation-audit-trail.md, 0019-reviewed-set-lock.md and 0027-pin-the-evaluation-trace-contract.md.
Also check rfcs/reviews/0002-round-2.md: that it reproduces the round-2 prompt and your output faithfully apart from the normalization its note declares (the originals are beside the worktree as prompt-0002-r2.txt, codex-0002-r2.md and codex-0002-r2.log), and that both findings have a disposition matching what the commit does. The disposition of R2-1 asserts that every sentence of the RFC mentioning the seam, entitlement constructs or an open choice was searched and read against the decision; test that assertion yourself with `grep -n -i -E "seam|entitlement|between.?systems|open one|stays open|prior question" rfcs/0002-judgment-graph.md` and report any sentence still inconsistent with the Scope section. Note that the retained introduction to Unresolved questions still says the questions "stay open" and names "one prior question"; a dated note immediately above it says that question is decided — say whether that treatment is sufficient or whether the retained sentence needs its own strike.

Then read the whole amended RFC once more, top to bottom, and CHANGELOG.md's entry and the rfcs/README.md index cell against it. If nothing material remains, say so plainly and return `mergeable as written`.

Verify against source, not the record's word. The reference runtime is a read-only snapshot of its origin/main (commit d891b05) at <snapshots>/runtime. Say what you could and could not run or verify.

For each finding give: severity (BLOCKER / MAJOR / MINOR), the exact sentence at issue, what the source says with file and line, and the smallest edit that would make the amendment true. Then a verdict: `mergeable as written`, `mergeable after fixes` (list them), or `not mergeable as written`. Do not propose new mechanisms, and do not argue what the graph format should be.
```

## Findings and verdict (verbatim)

**Verdict: `mergeable after fixes` — R3-1, one MINOR rendering issue.** No BLOCKER or MAJOR issue remains.

Reviewed `4335efc984df16d082988d1159266820500630db`, including `git log -3` and `git diff f76c519 4335efc`. Reviewer: OpenAI Codex; 2026-09-21.

The round-2 fixes check out:

- **R2-1 — originally MAJOR: substantively resolved; no new factual inaccuracy.** In the RFC (`rfcs/0002-judgment-graph.md:97`), the strike covers exactly the categorical between-systems clause and the open-choice sentence. The within-policy observation remains unchanged. Its preceding counts—four effect/entitlement cases and one action-completion/fan-in case—support that bounded characterization. Study 004’s own `RESULTS.md:15–16,59–82` and `PREREGISTRATION.md:26–46` corroborate the measurement and frame limitation. Both statements in the dated note are accurate: the between-systems case was outside the frame, and Scope explicitly decides dataflow composition while excluding effect/entitlement constructs (`RFC:166–188`). The paragraph remains coherent into “The usual bounds apply.” The multiline strike parses correctly with GFM-compatible strikethrough enabled; the parenthetical correctly italicizes only “Amended 2026-09-21.” The repository’s site renderer presents a separate problem below.

- **R2-2 — originally MINOR: resolved; no new inaccuracy.** “The runtime’s decisions on the reviewed-set lock, audit records and evaluation traces cover graphs too” accurately describes all three sources. Runtime `docs/adr/0019-reviewed-set-lock.md:82–92,112–128` includes graphs and their evaluation checks; `0018-opt-in-evaluation-audit-trail.md:73–81,89–104` includes graph node and composite records; `0027-pin-the-evaluation-trace-contract.md:90–99` expressly covers graph node traces. The revised sentence makes no incorrect claim about their single-pack origins.

The round-2 record is faithful under its declared normalization. Mechanical comparisons confirmed that its prompt matches `prompt-0002-r2.txt` and the normalized logged user prompt; its output matches `codex-0002-r2.md` after converting the local link to the declared plain relative-path citation. The logged final answer and repeated CLI answer also match the answer file. Both dispositions describe edits present in `4335efc`.

I ran the exact requested `grep` and read every matching sentence in context. **No operative sentence remains inconsistent with Scope.** The note at `RFC:289–293` explicitly identifies and supersedes the following paragraph’s prior-question wording, while preserving the five genuinely unresolved questions. That treatment is sufficient: “Each question stays open” and “one prior question” do **not** need another strike.

One finding remains:

**R3-1 — MINOR: the published-site renderer does not render either supersession strike.**

The exact sentence introducing the new note is:

> The struck passage did two things this RFC no longer does.

The passage includes this superseded sentence:

> Whether this RFC scopes composition to the between-systems seam or grows the edge toward entitlement constructs is now an evidenced choice, and still an open one.

**Source and observed behavior:** web/build.py:435 (`web/build.py:435`) publishes this RFC. Its renderer at lines 816–832 enables Python-Markdown’s `extra`, `sane_lists`, `toc`, and link rewriting, without strikethrough support. Running the whole RFC through that actual renderer in memory emitted both `~~…~~` spans literally, with no `<del>` or `<s>` elements. Thus the new passage at `RFC:100–104`, and the earlier correction at line 55, remain visually unstruck on the site. The dated qualifications still communicate the correction, so this is MINOR.

**Smallest edit:** replace the two existing pairs of `~~` delimiters with inline `<del>…</del>`, preserving their contents and notes. I verified that this replacement, in memory, renders both deletions correctly through the site renderer and a GFM-compatible parser, with emphasis intact.

The complete RFC reread found no further substantive inaccuracy. `CHANGELOG.md:111–124` and `rfcs/README.md:45` accurately summarize the amendment and preserve `Draft`.

Verification: 21 repository tests, five release tests, and three site tests passed; `git diff --check f76c519 4335efc` passed. The remaining release test’s two subtests and static-site setup could not run because no writable temporary directory was available. In-memory rendering checks succeeded. I did not run runtime Go tests or reproduce the study. The runtime snapshot lacks Git metadata, so its asserted `d891b05`/`origin/main` identity remains unverified; external links and public PR publication were not checked. The working tree remains unchanged.
## Maintainer disposition

One finding, minor, accepted and applied in the commit that adds this record.

| # | Finding | Disposition |
| --- | --- | --- |
| R3-1 MINOR | The site's Markdown renderer does not render `~~…~~`, so both supersession strikes in this RFC would appear as literal tildes on the published page | **Accepted**, with the reviewer's edit: both pairs of `~~` are replaced by `<del>…</del>`, contents and dated notes unchanged. Verified by building the site: the published page for this RFC carries two `<del>` elements and no literal tildes, and the text still renders as struck on GitHub. |

The finding is wider than this RFC, and the rest of it is not fixed here. Five other RFCs use `~~` — 0003, 0006 and 0007 from before this work, 0013 and 0014 from amendments made earlier the same day — and all of them show literal tildes on the published site. Rewriting their strikes would touch five RFCs to fix one renderer. The renderer is the right place, and that is a separate change to `web/build.py` with a test, which touches no RFC: tracked as a follow-up pull request, opened alongside this one.

Three rounds, nine findings — three major, six minor — every one accepted and applied. The reviewer confirmed in round 2 that the scope decision still decides after its evidential claims were narrowed, and in round 3 that no sentence of the RFC remains inconsistent with it. The maintainer's merge of the pull request is the adoption of the amendment; RFC 0002 remains a `Draft`.
