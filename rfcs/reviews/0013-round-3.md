# RFC 0013 — interim-review-regime record, round 3 (confirmation)

Reviewer: codex-cli 0.153.4, model gpt-6-astra (OpenAI), reasoning effort xhigh, read-only sandbox, run in a worktree of this repository at the reviewed commit, with read access to snapshots of the reference runtime's `origin/main` (`d891b05`) and of judgment-pack-evaluator-experiments' (`854653ea`), and to the files beside the worktree, among them the round-2 prompt, output and run log and the mutation script, which it used to check the round-2 record. It could write nothing; it reports what it could and could not run at the end of its output.
Different vendor than the model that drafted the change (Claude Fable 5.1, Anthropic), per RFC 0009 / GOVERNANCE.
Run: 2026-09-21, on commit `bfdc3d9`. One run; none discarded. Verdict: **mergeable as written** — no material finding; the four round-2 fixes confirmed.

> Note: absolute local paths are normalized here as in rounds 1 and 2. The reviewer's links into the worktree are written as plain `path:line` citations relative to the repository root — one of them, to the round-2 record, had no line and is written as a plain path; the prompt's paths to the two snapshots are written `<snapshots>/`. The prompt and the reviewer's output are otherwise verbatim and complete.

## Prompt (verbatim)

```
You are the interim-review-regime peer reviewer for the judgment-pack-spec repository, per RFC 0009 / GOVERNANCE.md — a model from a different vendor than the one that drafted the change (Claude). This is ROUND 3, a confirmation round. Under review is the branch at commit bfdc3d9 (`git log -3`), which accepts RFC 0013 and stages its five evaluation rows. Round 1 (on 8eb2fef) returned nine findings and round 2 (on d5ec45a) confirmed them fixed and returned four minor ones; rfcs/reviews/0013-round-1.md and 0013-round-2.md hold the verbatim prompts, your findings and the maintainer's disposition of each, and bfdc3d9 is the commit that applies round 2's (`git diff d5ec45a bfdc3d9`).

First verify each round-2 fix. Say per finding whether the edit resolves it, and whether it introduced a new inaccuracy or a new defect:
- R2-1: tests/test_site.py, test_staged_evaluation_rows_are_not_published, now matches whole path components. Check the component logic: does it match `artifacts/conformance/evaluation/staged/cases.json` and a page at `conformance/evaluation/staged/index.html`, and not `conformance/evaluation/staged-next/control.json`? Can it miss a leak — for instance does the published artifact path ever lack the `conformance/evaluation/staged` component run?
- R2-2: tests/test_release.py, test_release_bundle_leaves_staged_evaluation_rows_out. The control now covers every released corpus file and the ancestors `conformance`, `conformance/evaluation` and `conformance/evaluation/packs`, and where `git archive HEAD` succeeds the test lists the archive. Confirm your probe (an added `conformance/evaluation export-ignore` rule) now fails it. Read the archive branch closely: it returns early when `git archive` fails; it compares against `git ls-tree` so that an uncommitted released file is not demanded; it asserts no member under the staged directory. Is any of that a hole — in particular, is the early return a way for the test to pass while proving nothing in an environment that matters (CI checks out a commit; the release builder itself requires git)? Run it if you can: `python3 -m unittest discover -s tests -p test_release.py`.
- R2-3: the sentence in RFC 0013's appended *Security and privacy* bullet now reads that an implementation answering with a disposition "has done what §8.4 forbids: emitted a disposition for the evaluation, substituting `unresolved`, `not-applicable` or an outcome for the error." Check it against §8.4's exact words.
- R2-4: the comment in tests/test_repository.py's shared check. `git grep -n -i stricter -- ':!rfcs/reviews'` should now find no use of the word in this sense.
Also check rfcs/reviews/0013-round-2.md: that it reproduces the round-2 prompt and your output faithfully apart from the normalization its note declares (the originals are beside the worktree as prompt-0013-r2.txt, codex-0013-r2.md and codex-0013-r2.log), that every finding has a disposition matching what the commit does, and that the adoption record's "fifteen deliberate breaks ... two changes" agrees with the lists in the two records (fourteen and one in round 1's, one and one more in round 2's). The record states one limit of the mutation check itself — that its scratch copy has no commit, so the archive listing does not run there; say whether that limit is stated accurately.

Then read the whole change once more for anything rounds 1 and 2 missed. If nothing material remains, say so plainly and return `mergeable as written`.

Verify against source, not the record's word. Read-only snapshots: the reference runtime's origin/main (d891b05) at <snapshots>/runtime, and judgment-pack-evaluator-experiments' origin/main (854653ea) at <snapshots>/evaluator-experiments. Say what you could and could not run in the read-only sandbox.

For each finding give: severity (BLOCKER / MAJOR / MINOR), the exact sentence, line or row at issue, what the source says, and the smallest edit that would make the change true. Then a verdict: `mergeable as written`, `mergeable after fixes` (list them), or `not mergeable as written`. Do not propose new mechanisms beyond what a finding needs.
```

## Findings and verdict (verbatim)

All four round-2 **MINOR** findings are resolved at `bfdc3d9`. None introduced a new inaccuracy or defect requiring a fix. I found nothing material remaining in the full change.

| Finding | Confirmation |
|---|---|
| **R2-1 — MINOR** | **Resolved.** The component comparison (`tests/test_site.py:209`) catches both `artifacts/conformance/evaluation/staged/cases.json` and `conformance/evaluation/staged/index.html`, while allowing `conformance/evaluation/staged-next/control.json` and its artifact equivalent. I exercised the test with each path. |
| **R2-2 — MINOR** | **Resolved.** The expanded control (`tests/test_release.py:116`) covers the released manifest, schema, all four pack fixtures, and the three named ancestors. Supplying `conformance/evaluation export-ignore` through an in-memory attributes file now fails the test specifically at `conformance/evaluation`. |
| **R2-3 — MINOR** | **Resolved.** The revised sentence (`rfcs/0013-evaluator-error-and-precedence-cases.md:259`) accurately rests on §8.4’s “MUST NOT emit a disposition for that evaluation.” Although the following prohibition specifically names “a fallback outcome,” the preceding prohibition covers every outcome disposition. The inaccurate “by name” attribution is gone. |
| **R2-4 — MINOR** | **Resolved.** The comment (`tests/test_repository.py:698`) now acknowledges both newly admitted error rows and additional precedence checks. The requested `git grep` finds only RFC 0014’s unrelated “A stricter reading,” with no remaining use in this sense. |

No further edit is needed for these findings.

For R2-1, the publication-path concern is closed against the current builder. `copy_artifacts` preserves the complete repository-relative path beneath `artifacts/`; I enumerated its actual copy destinations with writes intercepted. A staged artifact leaking through that function would retain the `conformance/evaluation/staged` component run. The separate canonical-schema publisher has an explicit source list containing no staged file, and no staged page is registered. I found no current publication route that removes the component run and evades this test.

For R2-2, the real archive branch ran successfully, including `git ls-tree`. Comparing the entire conformance archive against tracked files showed **exactly the five staged files excluded**, with every other conformance file retained. An injected staged archive member failed the test. A simulated uncommitted released fixture passed appropriately: `git archive HEAD` cannot contain it, so the tracked-file condition is correct.

The early return accepts **any** nonzero archive exit, not solely an unborn `HEAD`. In that situation, success establishes the preceding attribute checks only. It does not prove nothing, and I found no demonstrated CI or release bypass: CI checks out a commit and runs this test; the release builder independently requires successful Git operations and checks archive failures. Here, the archive inspection actually executed.

The round-2 record (`rfcs/reviews/0013-round-2.md`) reproduces the supplied prompt and complete output exactly after the declared path normalization. The original output also matches the final output in the supplied log. All four dispositions match the commit.

The mutation accounting is correct: round 1 lists **fourteen breaks and one permitted change**; round 2 adds **one break and one permitted change**. Its stated limitation is accurate. The supplied mutation script excludes `.git`, initializes a fresh repository, and never commits. Consequently, its archive inspection cannot run, while the ancestor attribute check catches break 15. The historical full-suite mutation results remain attestations, as labeled.

The broader reread found the five expected classes consistent with §§8.2 and 8.4. The single-outcome fixture has exactly `JPS-STRUCTURE-COLLECTION-ARITY` at `/outcomes`; the extension fixture otherwise conforms; the triage copy is byte-identical. The released manifest, schema, and packs are unchanged against `v0.2.0-draft`. The staging, versioning, acceptance, and governance explanations remain consistent with their sources.

Validation and limits:

- **20 repository tests passed.**
- **Five release tests passed**, including the real archive inspection. The sixth test’s two subcases failed because the sandbox cannot create temporary directories.
- **Three site field-reference tests passed.** Full static-site setup was blocked by the same restriction; the targeted path probes ran in memory.
- All five cases raised their expected preflight errors through the supplied Python evaluator API. Removing case 2’s undeclared key produced `not-applicable`.
- Runtime source and its precedence test support the ordering claim. Executing that Go test was blocked when Go tried to create its temporary build directory.
- The supplied snapshots lack Git metadata, so their stated commit identities could not be independently resolved. No files changed.

**Verdict: `mergeable as written`.**
## Maintainer disposition

No finding to disposition. Three rounds, thirteen findings — three major and ten minor — every one accepted and applied. One of the reviewer's observations is recorded for whoever next touches the release test: its early return accepts any nonzero exit of `git archive`, not only the absence of a commit, and in that case the test establishes the attribute checks and not the archive's contents. The reviewer found no CI or release path that this lets through — CI checks out a commit and runs the test, and the release builder requires its own git operations to succeed — so it is left as it stands.

The maintainer's merge of the pull request is the acceptance of RFC 0013, with its five rows staged and in no corpus.
