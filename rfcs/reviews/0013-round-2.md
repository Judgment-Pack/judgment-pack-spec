# RFC 0013 — interim-review-regime record, round 2 (confirmation)

Reviewer: codex-cli 0.153.4, model gpt-6-astra (OpenAI), reasoning effort xhigh, read-only sandbox, run in a worktree of this repository at the reviewed commit, with read access to snapshots of the reference runtime's `origin/main` (`d891b05`) and of judgment-pack-evaluator-experiments' (`854653ea`), and to the files beside the worktree, among them the round-1 prompt, output and run log, which it used to check the round-1 record. It could write nothing; as it reports, it ran the twenty repository tests, five release tests and three site field-reference tests, and could not run the tests that need a temporary directory.
Different vendor than the model that drafted the change (Claude Fable 5.1, Anthropic), per RFC 0009 / GOVERNANCE.
Run: 2026-09-21, on commit `d5ec45a`. One run; none discarded. Verdict: **mergeable after fixes** — all nine round-1 findings confirmed fixed, the three major ones without a new defect; four new minor findings, two of them in the tests round 1's fixes added.

> Note: absolute local paths are normalized here as in round 1. The reviewer's links into the worktree are written as plain `path:line` citations relative to the repository root; the prompt's paths to the two snapshots are written `<snapshots>/`. The prompt and the reviewer's output are otherwise verbatim and complete.

## Prompt (verbatim)

```
You are the interim-review-regime peer reviewer for the judgment-pack-spec repository, per RFC 0009 / GOVERNANCE.md — a model from a different vendor than the one that drafted the change (Claude). This is ROUND 2, a confirmation round. Under review is the branch at commit d5ec45a (`git log -2`), which accepts RFC 0013 and stages its five evaluation rows. Round 1 reviewed its parent 8eb2fef and returned nine findings, three of them major; rfcs/reviews/0013-round-1.md holds the verbatim prompt, your findings and the maintainer's disposition of each, and d5ec45a is the commit that applies them (`git diff 8eb2fef d5ec45a`).

First verify each round-1 fix against the source named in its finding. Say per finding whether the edit makes the change true, and whether it introduced a new inaccuracy or a new defect:
- F1: web/build.py now has UNPUBLISHED_ARTIFACT_DIRECTORIES and is_published_artifact, and copy_artifacts skips conformance/evaluation/staged/. Check the path logic is correct on every supported Python (it deliberately avoids PurePath.is_relative_to) and cannot skip anything else — for example a sibling directory whose name begins with "staged". tests/test_site.py gains test_staged_evaluation_rows_are_not_published and an exclusion in the byte-for-byte copy test; check the new test would fail if the build published a staged file, and that the exclusion in the old test cannot hide a genuinely unpublished released artifact. A new .gitattributes marks the staged directory export-ignore, because tools/build_release.py builds the bundle with `git archive`; verify with `git archive HEAD conformance | tar -t` that no staged member is archived and that the released corpus still is, and read tests/test_release.py's new test_release_bundle_leaves_staged_evaluation_rows_out — does it test the mechanism, is its control meaningful, and is its skip condition a hole? Does .gitattributes affect anything else in the repository? Finally read the staged README's two paragraphs on publication and on how a row lands: are they now true?
- F2: tests/test_repository.py now rejects PREFLIGHT_ERROR_CLASSES rather than whitelisting two results. Confirm your probe (a released row expecting `com.example.timeout`) passes the check, and that a released row wrongly expecting `malformed-input` with no undeclared key still fails.
- F3: the staged README, the adoption record in RFC 0013 and CHANGELOG.md on what a disagreement with a staged row means. Is the new statement faithful to Core §3.4 and §3.4.1, and does it now overclaim in the other direction — for instance by implying a staged row is normative?
- F4: the restated cost of a new specVersion, against Core §11 and VERSIONING.md.
- F5: case 2's `focus` in conformance/evaluation/staged/cases.json, the staged README paragraph about it, and the appended *Security and privacy* and *Conformance* bullets in RFC 0013. Check the sentence that an implementation answering with a disposition "has done what §8.4 forbids by name: substituted `unresolved`, `not-applicable` or an outcome for the error" against §8.4's exact words. Confirm cases.json still validates against the released case schema.
- F6: the rewritten lapse paragraph against GOVERNANCE.md's Scope, Record and pull-requests-only bullets.
- F7: test_staged_non_conformant_fixture_fails_for_its_one_stated_reason. Confirm your scenario (a second outcome added, the rule pointed at an undeclared outcome) now fails it.
- F8: the struck prior-art sentence and its dated amendment, against conformance/manifest.json.
- F9: the new paragraph in rfcs/README.md separating acceptance from incorporation, against RFC 0000.
Also check rfcs/reviews/0013-round-1.md: that it reproduces the round-1 prompt and your output faithfully apart from the normalization its note declares (the original prompt, output and run log are beside the worktree as prompt-0013-r1.txt, codex-0013-r1.md and codex-0013-r1.log), that every finding has a disposition matching what the commit does, and that the mutation list it gives is described as an attestation and not as something the repository shows. The adoption record now says "fourteen deliberate breaks ... were each caught, and ... one change the check must not refuse was not" — check the record's list has fourteen and one.

Then read the whole change again for anything round 1 missed: the five rows against Core §8.2 and §8.4 once more, the two fixtures, the shared check, every new test, the RFC's adoption record and appended sections, the staged README, conformance/evaluation/README.md, rfcs/README.md, web/build.py and CHANGELOG.md. You may run `python3 -m unittest discover -s tests -p test_repository.py`; the site and release tests need a writable temporary directory, which the read-only sandbox may not give you — say what you could and could not run. If nothing material remains, say so plainly and return `mergeable as written`.

Verify against source, not the record's word. Read-only snapshots: the reference runtime's origin/main (d891b05) at <snapshots>/runtime, and judgment-pack-evaluator-experiments' origin/main (854653ea) at <snapshots>/evaluator-experiments.

For each finding give: severity (BLOCKER / MAJOR / MINOR), the exact sentence, line or row at issue, what the source says, and the smallest edit that would make the change true. Then a verdict: `mergeable as written`, `mergeable after fixes` (list them), or `not mergeable as written`. Do not propose new mechanisms beyond what a finding needs.
```

## Findings and verdict (verbatim)

The three round-1 major findings are substantively fixed. **Four MINOR issues remain**, concerning the new tests and two accuracy claims. No BLOCKER or MAJOR remains.

| Round-1 finding | Confirmation |
|---|---|
| **F1 — MAJOR** | **Publication fixed; two new test defects below.** The build compares complete path components, so `staged-next/` and `staged.json` remain publishable. It works on the available Python 3.8.20 and uses no API unavailable on CI’s Python 3.12; the repository declares no minimum version. The old copy test excludes only `staged/`, leaving every released artifact checked. The new absence test detects leaked staged JSON and pages, but also rejects siblings. The committed archive exclusion works; its test’s released control is incomplete. |
| **F2 — MAJOR** | **Fixed; no new defect found.** A released row expecting `com.example.timeout` passes both the schema and shared check. The same row unjustifiably expecting `malformed-input` fails. Adding an undeclared key while retaining its disposition also still fails. This matches §8.4’s Core precedence and allowance for implementation-defined classes. |
| **F3 — MAJOR** | **Fixed; no new normative obligation introduced.** The README distinguishes unchanged corpus obligations from the existing §§8.2–8.4 contract, expressly allows the staged row itself to be wrong, and says it is not binding. Read together, the adoption record and changelog make the same distinction. Their “optional reading” wording does not make staged rows normative. |
| **F4 — MINOR** | **Fixed; no new defect found.** The migration cost is now limited to artifacts targeting the new version and implementations choosing it. Core §11 and VERSIONING preserve older-version checkability and prohibit inheriting conformance claims across versions. |
| **F5 — MINOR** | **Substantively fixed; one new attribution inaccuracy below.** Case 2’s facts make applicability false, exactly the §8.2 trap now described. All five cases still validate against the released case schema. |
| **F6 — MINOR** | **Fixed; no new defect found.** “Discloses, and cannot undo” correctly distinguishes present scrutiny from PR 64’s historical violation. GOVERNANCE’s Scope, Record and Pull-requests-only bullets attach the obligation to the original PR. |
| **F7 — MINOR** | **Fixed; no new defect found.** The test pins one outcome and exactly `JPS-STRUCTURE-COLLECTION-ARITY` at `/outcomes`. Adding a second outcome and redirecting the rule to an undeclared outcome now fails it. |
| **F8 — MINOR** | **Fixed; no new defect found.** Both manifest entries expect `invalid`. The dated amendment correctly describes complementary negative cases. |
| **F9 — MINOR** | **Fixed; no new defect found.** The added paragraph explicitly separates approval from incorporation, consistent with RFC 0000’s definition of acceptance. RFC 0013’s staged status is clear. |

The remaining findings are:

1. **R2-1 — MINOR: the new publication test rejects permitted siblings.**

   tests/test_site.py:210 (`tests/test_site.py:210`) uses:

   ```python
   if "evaluation/staged" in path.relative_to(self.output).as_posix()
   ```

   The build and staged README exclude the specific directory `conformance/evaluation/staged/`. This substring also matches `artifacts/conformance/evaluation/staged-next/control.json`. An in-memory output containing that permitted sibling made the test fail, although `is_published_artifact` correctly permits it.

   **Smallest edit:** match complete `evaluation` and `staged` path components instead of an unrestricted substring.

2. **R2-2 — MINOR: the release test’s control does not establish that the released manifest survives archiving.**

   tests/test_release.py:129 (`tests/test_release.py:129`) asserts:

   ```python
   self.assertEqual("unspecified", attributes.get(released))
   ```

   `git check-attr` reports the attribute on that path; it does not establish that an ancestor directory survives `git archive`. I supplied an additional `conformance/evaluation export-ignore` rule through an in-memory attributes input. **The test passed, while the archive omitted the released manifest.**

   The test checks the actual exclusion mechanism’s configuration, but its control misses directory-level exclusion.

   **Smallest edit:** also check that `conformance` and `conformance/evaluation` are not export-ignored, or assert the released manifest’s presence in an actual archive listing.

   The skip condition did not trigger here. It can leave this check unexecuted where Git fails, but missing or unusable Git also prevents the release builder from producing a bundle; it is not a demonstrated publication bypass.

3. **R2-3 — MINOR: “by name” inaccurately attributes the expanded outcome wording to §8.4.**

   RFC 0013:259 (`rfcs/0013-evaluator-error-and-precedence-cases.md:259`) says:

   > what §8.4 forbids by name: substituted `unresolved`, `not-applicable` or an outcome for the error.

   Core §8.4:695 (`spec/judgment-pack-core.md:695`) specifically names **“a fallback outcome.”** Its preceding prohibition on emitting any disposition supports the RFC’s broader substantive conclusion, but not that exact “by name” attribution.

   **Smallest edit:** delete **“by name.”** Merely changing “an outcome” to “a fallback outcome” would incorrectly narrow the sentence’s assertion about *any* disposition.

4. **R2-4 — MINOR: the review record overstates completion of the “stricter” wording cleanup.**

   review record:157 (`rfcs/reviews/0013-round-1.md:157`) says:

   > “Stricter than before” is scoped everywhere it appears

   However, tests/test_repository.py:701 (`tests/test_repository.py:701`) still asserts unqualifiedly that the check “is stricter than the one it replaces.” The accepted input set intentionally expands to admit correctly labelled undeclared-key error rows.

   **Smallest edit:** update that remaining comment to acknowledge the newly admitted error rows and the added precedence checks, matching the revised prose.

The actual publication boundary is correct. `git archive HEAD conformance | tar -t` contains **no staged member**, while preserving the released manifest, carrier and all four released evaluation fixtures. Comparing archive members with tracked files showed exactly the five staged files excluded and every other conformance file retained. No other tracked path acquires `export-ignore`. The attribute affects Git archives generally, not just the curated release bundle; it does not remove files from ordinary checkouts. The staged README’s publication and landing descriptions are now consistent with these mechanisms and the versioning policy.

The five rows remain correct against §§8.2 and 8.4:

| Case | Required class | Additional condition |
|---|---|---|
| 1 | `pack-not-conformant` | `{}` is legal evidence availability; no second pack defect. |
| 2 | `malformed-input` | Applicability is false; omitted evidence keys mean unknown. Neither bypasses preflight. |
| 3 | `unsupported-required-extension` | Pack and evidence otherwise conform. |
| 4 | `pack-not-conformant` | Undeclared evidence also makes `malformed-input` applicable. |
| 5 | `malformed-input` | Unsupported required extension also applies, later in precedence. |

The single-outcome fixture has exactly one structural diagnostic at `/outcomes` and no semantic diagnostics. The extension fixture has neither structural nor semantic diagnostics and supplies the required extension value under §9. The copied triage fixture is byte-identical. The released manifest, schema and packs remain unchanged, including against `v0.2.0-draft`.

I independently ran all five through the supplied Python evaluator API: each raised the expected preflight error without returning a disposition. Removing case 2’s undeclared key produced `not-applicable`. The Go source and precedence test support the adoption record’s ordering claim; I did not execute Go. The Python precedence test uses non-object evidence, and the agreement harness compares dispositions rather than error classes, as recorded. Historical manual runs remain attestations.

The review record reproduces the original prompt and output exactly after the declared path normalization; the original output also appears verbatim in the supplied log. All nine disposition entries correspond to changes made by `d5ec45a`, subject to the new issues above. The mutation history is expressly an attestation, with **fourteen numbered breaks and one permitted change**, matching the adoption record.

Validation completed:

- **20 repository tests passed.**
- **Five release tests passed**, including the new attribute test; the remaining test’s two subcases could not create temporary directories.
- **Three site field-reference tests passed**; `StaticSiteTests` could not create its temporary directory.
- Read-only archive checks and in-memory mutation probes completed. No files changed.

**Verdict: `mergeable after fixes` — R2-1 through R2-4.**
## Maintainer disposition

Four findings, all minor, all accepted and applied in the commit that adds this record. None is rejected or deferred.

| # | Finding | Disposition |
| --- | --- | --- |
| R2-1 MINOR | The not-published site test matched the substring `evaluation/staged`, so a permitted sibling such as `staged-next/` would make it fail | **Accepted.** The test now matches whole path components, as the build's own `is_published_artifact` does. The reviewer's probe — a sibling `conformance/evaluation/staged-next/control.json`, which the build publishes — leaves the suite green. |
| R2-2 MINOR | The release test's control checked the released manifest's own attribute, which a directory-level `export-ignore` leaves "unspecified" while the archive drops the file | **Accepted**, with both of the reviewer's suggestions. The control now covers every released corpus file and their ancestor directories, and where there is a commit to archive the test lists what `git archive HEAD conformance/evaluation` actually contains: no staged member, and every tracked released corpus file. The reviewer's probe — a `conformance/evaluation export-ignore` rule — now turns the suite red. |
| R2-3 MINOR | "What §8.4 forbids by name" attributed to §8.4 a wording it does not have; §8.4 names "a fallback outcome" | **Accepted**, by the reviewer's edit and not the narrower one it warns against. "By name" is deleted, and the sentence now rests on §8.4's prohibition on emitting any disposition for the evaluation, so it still covers an outcome of any kind. |
| R2-4 MINOR | The round-1 record said "stricter than before" was scoped everywhere it appears; a comment in `tests/test_repository.py` still said it unqualified | **Accepted.** The comment now says what the prose says: the check admits a correctly labelled error row, which the old one refused, and asks more of what it still covers. A search of the tree outside the review records finds no other use of the word in this sense. The round-1 record's sentence was wrong when written and is left as it stands, corrected here. |

### The mutation check, brought up to date

Still an attestation: the script is not in this repository. After the fixes above, the fourteen breaks and one permitted change listed in the round-1 record were run again with the full suite, together with the reviewer's two round-2 probes:

15. A break: `.gitattributes` gains `conformance/evaluation export-ignore`, which would drop the released corpus from a bundle. Caught, by the release test.

And a second change that must **not** turn the suite red, and did not: a sibling directory `conformance/evaluation/staged-next/` holding a JSON file, which the site build publishes.

Fifteen breaks caught, two permitted changes left green. One limit: the scratch copy the script works on has no commit, so the release test's archive listing does not run there and break 15 is caught by the ancestor attribute check alone. The archive listing runs in this worktree and in CI, where there is a commit.

The fixes are changes that implement dispositioned findings of a review recorded on the same pull request, which GOVERNANCE excepts from a fresh review. Two of them change tests, so a third round is run to confirm them and recorded as round 3.
