# RFC 0004 — interim-review-regime record, round 2 (confirmation)

Reviewer: codex-cli 0.153.4, model gpt-6-astra (OpenAI), reasoning effort xhigh, read-only sandbox, run in a worktree of this repository at the reviewed commit, with read access to a snapshot of the reference runtime's `origin/main` (`d891b05`) and to the files beside the worktree, among them the round-1 prompt, output and run log, which it used to check the round-1 record. It could write nothing.
Different vendor than the model that drafted the change (Claude Fable 5.1, Anthropic), per RFC 0009 / GOVERNANCE.
Run: 2026-09-21, on commit `4316256`. One run; none discarded. Verdict: **mergeable as written** — no material finding; the seven round-1 fixes confirmed.

> Note: absolute local paths are normalized here as in round 1. The reviewer's links into the worktree are written as plain `path:line` citations relative to the repository root; the prompt's one path to the runtime snapshot is written `<snapshots>/runtime`. The prompt and the reviewer's output are otherwise verbatim and complete.

## Prompt (verbatim)

```
You are the interim-review-regime peer reviewer for the judgment-pack-spec repository, per RFC 0009 / GOVERNANCE.md — a model from a different vendor than the one that drafted the change (Claude). This is ROUND 2, a confirmation round. Under review is the branch at commit 4316256 (`git log -2`), which dispositions RFC 0004 (Planner interface) as `Rejected`. Round 1 reviewed its parent c5d72e4 and returned seven findings; rfcs/reviews/0004-round-1.md holds the verbatim prompt, your findings and the maintainer's disposition of each, and 4316256 is the commit that applies them (`git diff c5d72e4 4316256`).

First verify each round-1 fix against the source named in its finding. Say per finding whether the edit makes the change true, and whether it introduced a new inaccuracy:
- F1: the dated clarification in RFC 0006's adoption record, and the sentence in RFC 0004's record that now describes it. Check the quotation "stay with" against RFC 0006, and that the historical adoption decision is unchanged.
- F2: the site description in web/build.py.
- F3: the bounded authorization sentence in RFC 0004's record.
- F4: the new heading of the runtime bullet.
- F5: the attributed and bounded negative evidence — including the quotation from GOVERNANCE.md ("means a change sits in public and no one comments": check it is exact and fairly used), the statement about `packs list` and `list_packs` (verify against the runtime snapshot: is it an explicitly non-normative inventory, and does it take no decision context?), and the changelog wording.
- F6: rfcs/reviews/0004-round-1.md now exists. Check that it reproduces the round-1 prompt and your output faithfully apart from the path normalization its note declares, that the normalization is what the note says and nothing more, and that every finding has a disposition.
- F7: README.md, the two web/build.py descriptions and FAQ Q23.
Then read the whole change again for anything round 1 missed — in particular, confirm once more that every section of RFC 0004 from `## Summary` to the end is byte-identical to c5d72e4~1, and check CHANGELOG.md's entry against what the commits actually do. If nothing material remains, say so plainly and return `mergeable as written`.

Verify against source, not the record's word. The reference runtime is a read-only snapshot of its origin/main (commit d891b05) at <snapshots>/runtime — docs/adr/0012-jpack-project-convention.md, docs/adr/0007-experimental-evaluator.md, internal/cli/packs.go, internal/project/, README.md.

For each finding give: severity (BLOCKER / MAJOR / MINOR), the exact sentence or line at issue, what the source says, and the smallest edit that would make the change true. Then a verdict: `mergeable as written`, `mergeable after fixes` (list them), or `not mergeable as written`. Do not propose new mechanisms and do not argue whether a planner should be standardized in the abstract.
```

## Findings and verdict (verbatim)

No material findings remain at `4316256`. All seven fixes resolve their round-1 findings without introducing a new inaccuracy. Severities below are the original findings’ severities; **no further edit is required for any**.

1. **F1 — MAJOR, resolved.**  
   RFC 0006:27 (`rfcs/0006-evaluator-conformance.md:27`) now says: “(*Amended 2026-09-21:* RFC 0004 has since been rejected.” It then preserves the selection-probe note as informative product advice and states that no active proposal carries it. The original adoption record actually says **“stay with”**, so RFC 0004:52–54 quotes it accurately. Removing only the dated insertion reproduces RFC 0006’s previous bytes exactly: the historical adoption decision and technical advice are unchanged.

2. **F2 — MAJOR, resolved.**  
   web/build.py:456 (`web/build.py:456`): “Rejected proposal: the project declines to standardize planner selection, including the proposed selection query.” This accurately describes the disposition. RFC 0004:48–49 still leaves portability unanswered; the description no longer claims to answer standardizability.

3. **F3 — MAJOR, resolved.**  
   RFC 0004:40 (`rfcs/0004-planner-interface.md:40`): “Declining adds no standardized selection surface; it does not remove the confusion, since Core still defines applicability and a product may still build a selector, and whether standardizing would have made the confusion worse stays untested.” Core defines applicability; the retained RFC’s security section and RFC 0005:51–53 distinguish applicability or selection metadata from authorization. The revised sentence respects those boundaries and makes no unsupported causal claim.

4. **F4 — MINOR, resolved.**  
   RFC 0004:30: “The reference runtime deliberately leaves selection with the application.” Runtime ADR-0012:115–122 expressly establishes that boundary and gives applicability versus authorization as its reason. Its quotation—“does not choose a pack for a request, and this runtime never will”—is exact. ADR-0007:70–72 expressly excludes planner selection from the evaluator. The new heading accurately states their scope.

5. **F5 — MINOR, resolved.**  
   RFC 0004:18–29 now says “To the maintainer’s knowledge,” “no producer of either format was found in this project’s repositories,” and describes the runtime inventory as “explicitly non-normative” and taking “no decision context.”

   These qualifications are supported. RFCs 0001 and 0005 remain Draft; the inspected sources provide no producer or implementation evidence for the proposed formats/query. Runtime `internal/project/inventory.go:166–208` enumerates configured packs and sets `Kind: result.ProjectKind`, whose value is `non-normative-runtime-convention`. `packs list` accepts only configuration/output flags; `list_packs` declares an empty argument schema and rejects arguments. Neither accepts decision context or ranks applicability, and neither emits the proposed manifest/index format.

   “means a change sits in public and no one comments” matches GOVERNANCE.md:53–54 (`GOVERNANCE.md:53`) exactly across the line wrap. Attribution to governance’s account is fair. CHANGELOG.md:95 likewise correctly qualifies the demand claim as the maintainer’s knowledge.

6. **F6 — MINOR, resolved.**  
   The review archive (`rfcs/reviews/0004-round-1.md:7`) exists, and its “otherwise verbatim and complete” claim checks out against the original `prompt-0004-r1.txt`, `codex-0004-r1.md`, and raw run log found beside the worktree. There is exactly one runtime-path substitution in the prompt and nine conversions of absolute worktree links into relative `path:line` citations. No other prompt or output changes were found. All seven findings have written acceptance dispositions at lines 115–121, and the corresponding edits are present. The separate PR-comment copy was not independently verified.

7. **F7 — MINOR, resolved.**  
   README.md:155 now says “Change proposals (RFCs), their dispositions, and the process.” The two collection descriptions in web/build.py:412 and :1980 likewise include disposition records. Their non-normative characterization agrees with Core §1.1 and the RFC index, including accepted RFCs whose resulting normative text lives elsewhere.

   FAQ Q23:135–137 (`FAQ.md:135`) explicitly distinguishes still-draft discovery formats from rejected standardized selection. That accurately states the current disposition without foreclosing a future evidence-backed RFC.

The full-change recheck found no additional material issue. RFC 0000 permits this rejection, and the index, seeded-proposals list, architecture description and site label agree. RFC 0005’s remaining selection-interface question is openly acknowledged in the disposition rather than silently treated as resolved.

Everything from `## Summary` through EOF in RFC 0004 is **byte-identical to `c5d72e4~1`**, also matching `c5d72e4`: 2,608 bytes, SHA-256 `b2d35453d651b6ecfd33318b86638f4b69cffc83f39a89537defccb7a6ff0f6d`.

CHANGELOG.md:92–100 accurately describes the combined commits: rejection, bounded rationale, unanswered portability question, and the listed documentation updates. No normative artifact changes. `git diff --check` passes.

**Verdict: `mergeable as written`.**
## Maintainer disposition

No finding to disposition. Two rounds, seven findings, every one accepted and applied. The reviewer notes that it did not independently verify the pull-request copy of the record; the pull request's description summarizes both rounds and points here, and these files are the complete record. The maintainer's merge of the pull request is the rejection of RFC 0004.
