# RFC 0011 — interim-review-regime record, round 5 (second amendment)

Rounds 1–2 (the pre-merge review of the RFC body) are in [`0011-round-1.md`](0011-round-1.md);
rounds 3–4 (the first amendment: Unresolved #8/#9) are in [`0011-round-3.md`](0011-round-3.md).
This round reviews the **second amendment** — Specification §2a and Unresolved #10/#11 — and
nothing else.

Reviewer: codex-cli 0.145.0, model gpt-5.6-sol (OpenAI), reasoning effort ultra, read-only sandbox.
Different vendor than the model that drafted the amendment (Claude), per RFC 0009 / GOVERNANCE.
Run: 2026-08-11. Verdict: **mergeable after listed fixes** (5 MAJOR, 4 MINOR).

> **Record-keeping deviation, stated plainly.** This review was still running when the amendment
> merged as `9a77e94` (PR #53), so — unlike every prior round in this record — its findings and
> dispositions land *after* the text became governing rather than before. The reviewed bytes are
> the merged bytes: the worktree the reviewer read and `origin/main` hashed identically
> (`38eed926…`) at review time, so the findings apply to what shipped without qualification. The
> fixes below are therefore delivered as a follow-up pull request against the merged text. The
> regime's substance — an independent reviewer, recorded verbatim, with a written disposition per
> finding — is preserved; its ordering was not.

> Note: the prompt's worktree paths are normalized to `<spec worktree>/`; the finding prose is
> otherwise verbatim.

## Prompt (verbatim)

```
You are the interim-review-regime peer reviewer for the judgment-pack-spec repository, per
RFC 0009 / GOVERNANCE.md — a model from a different vendor than the one that drafted the
change. Under review is a second AMENDMENT to the already-merged RFC 0011: a new
Specification subsection 2a (a scope statement separating the registry's job from a relying
organization's "transition rule"), plus two new Unresolved questions (#10 where transition
rules live; #11 whether consuming artifacts should cite the registry head they observed).
Nothing else changed.

Your job is to find where the amendment overclaims, misrepresents another document,
smuggles a design decision into an open question, contradicts the merged text around it, or
narrows in a way that quietly breaks something the RFC already promised. A clean pass is a
finding only if you can defend it.

Read, in the spec worktree at
<spec worktree>/:

- rfcs/0011-judgment-currency-anchor.md — the amended RFC. `git diff 2cdb0a7..HEAD` shows
  exactly the delta; everything else is merged text that already passed four review rounds.
- rfcs/reviews/0011-round-1.md — rounds 1-2 (the pre-merge review of the body). Its
  dispositions bound what the RFC may claim; check the new text hardest against R-7
  (membership at snapshot only, never a real-time or usability verdict), R-11 (a public
  remark is motivating context, never evidence of convergence or merit), R-2 (every
  proposed part must land in a named non-JPS repository; nothing may read as a JPS Core
  change), and R-12 (an outcome is a declared result, not authorization).
- rfcs/reviews/0011-round-3.md — rounds 3-4 (the first amendment's review). Its RA-1..RA-3
  dispositions are the standard the new items must meet: no answers asserted inside
  questions, no categorical claims, related-not-identical when comparing to other records.
- rfcs/0000-rfc-process.md — the cross-project exploratory provision and required sections.
- rfcs/0010-gateway-signing-identity.md and spec/judgment-pack-core.md sections 5, 10, 11,
  13 — verify any claim the amendment makes about them.

Scrutinize, at minimum:

1. Is Specification 2a a legitimate scope NARROWING, or does it add a normative
   requirement in disguise? It says the format "carries no expiry, grace, or grandfathering
   semantics" and that the verdict "stays exactly as narrow as it is" — are those consistent
   with the merged Specification sections 1 and 2, or do they retroactively restate them?
2. Does 2a contradict, duplicate, or quietly weaken anything already in the merged text —
   the Summary, Specification section 2's narrow-verdict paragraph, Security's freshness
   floor, or the "states currency, not correctness/authorization" sentence in section 1?
3. Is the three-way example (immediate expiry / grace window / grandfathering) accurate as
   a claim that organizations legitimately differ, or does it assert a governance opinion
   the RFC has no basis for? Is "none of these is more correct" defensible in an
   exploratory record?
4. Unresolved #10: is it genuinely open, or does it answer itself? Look especially at the
   closing sentence about the asymmetry ("a verifier that reports only membership can serve
   every transition rule, while a verifier that reports a verdict can serve only one") — is
   that an open question, an argument, or a smuggled requirement? Is the "non-binding hint"
   sub-question posed neutrally?
5. Unresolved #11: does it stay descriptive about the cited-head construction? Check every
   claim about what it "buys": the two-sided fact, the fail-fast property for honest
   producers, and the three stated limits (no ordering; a re-minted artifact may cite an
   older head; the citation inherits the split view of #9). Is the Study 014 reference
   accurate — verify against
   https://github.com/Judgment-Pack/judgment-pack-evaluator-experiments/tree/main/studies/014-openworkproof-binding
   if reachable, else say so — and is the build/commit analogy sound or does it overreach?
6. Attribution and evidence discipline: the amendment cites a public thread as motivating
   context and names the participant by handle. Does that meet R-11's standard, or does any
   sentence let a public remark do evidentiary work? Is crediting by handle appropriate
   here, and is the disclaimer sufficient and correctly placed?
7. Placement (RFC 0000): does anything in 2a, #10, or #11 propose work that would land in
   JPS Core, a profile, or this repository, contrary to the RFC's own scope note?
8. Consistency of the whole Unresolved list now that it has eleven items: do #10/#11
   overlap with #1 (who signs), #3 (freshness/ordering), #5 (portable vs per-protocol), or
   #9 (split views) in a way that muddies rather than adds? Should any be merged or
   cross-referenced instead?
9. Required-sections completeness per RFC 0000 for the amended document, and whether 2a
   belongs in Specification at all rather than in Compatibility or a new section.

Output, exactly:
- Numbered findings RB-<n>, each: severity BLOCKER / MAJOR / MINOR, the section, a
  one-paragraph description of the failure mode, and a concrete fix.
- Then one line: `mergeable as written`, `mergeable after listed fixes`, or
  `not mergeable as written`.
Findings only. Cite the file/section you read for every claim; if a cross-repository claim
could not be verified, say so explicitly rather than guessing.
```

## Findings (verbatim, unedited)

1. RB-1 — MAJOR — Section: Summary, Specification §2a, Security “freshness floor.” Section 2a makes snapshot membership only an input to continued reliance, yet the Summary still calls a reused decision under a retired pack “simply out of date,” and Security says a pre-snapshot retirement is “caught.” Under §2a’s own grace and grandfathering examples, only the tuple’s nonmembership is universal; usability is unresolved. Fix: describe the tuple solely as “not current at the observed snapshot” and defer continued reliance explicitly to an external transition rule. (`rfcs/0011-judgment-currency-anchor.md`, Summary, Specification §§2–2a, Security and privacy; `rfcs/reviews/0011-round-1.md`, R-7 disposition.)

2. RB-2 — MAJOR — Section: Specification §2a. “Belongs to the relying organization,” “not to the consumer step,” “none … is more correct,” and “whoever bears the risk” go beyond scope narrowing: they categorically assign governance and implementation ownership. A rule may be constrained by an external legal, contractual, or issuing authority, and consumer software may apply a configured rule while preserving the raw membership result. Core assigns no such transition authority. Fix: state only that this registry and §2’s membership primitive do not determine continued reliance; leave the rule’s source, binding force, placement, and evaluation open. (`rfcs/0011-judgment-currency-anchor.md`, Specification §2a and Unresolved #10; `spec/judgment-pack-core.md`, §§5, 10, 11, 13; `rfcs/reviews/0011-round-3.md`, RA-1–RA-3 dispositions.)

3. RB-3 — MINOR — Section: Specification §§1 and 2a. “The format … carries no expiry … semantics” is facially broader than intended because §1’s `retire` event makes a version leave the supported set at a defined position. Fix: say the format carries no “decision-level expiry, grace, or grandfathering semantics” or no semantics for continued reliance on an existing decision. (`rfcs/0011-judgment-currency-anchor.md`, Specification §§1 and 2a.)

4. RB-4 — MAJOR — Section: Specification §2a and Unresolved #10. The “non-binding hint” option is not genuinely open under §2a: a recommended grace or transition alongside retirement necessarily carries transition semantics, which §2a categorically excludes. Calling hardening “the failure mode this separation exists to prevent” argues against the option inside the question, while “an authority” assumes the currency signer may recommend transition policy despite Unresolved #1 distinguishing publishers from neutral logs that attest only inclusion. Fix: remove the registry-hint branch, or limit §2a’s prohibition to authoritative/enforced transition semantics and neutrally ask who issues, signs, labels, and owns optional advice. (`rfcs/0011-judgment-currency-anchor.md`, Specification §2a, Security and privacy, Unresolved #1/#10; `rfcs/reviews/0011-round-1.md`, R-2/R-9 dispositions; `rfcs/0000-rfc-process.md`, “Cross-project exploratory records.”)

5. RB-5 — MAJOR — Section: Unresolved #10. The closing “asymmetry” is a categorical answer and is unsound at its stated breadth. Section 2 already calls membership a “verdict”; membership at one snapshot cannot by itself serve grace or grandfathering rules needing the trusted ordering that §2 and #3 say is absent, while a parameterized verifier can expose raw membership and calculate more than one separately labeled derived result. Fix: delete the sentence or say conditionally that exposing raw membership avoids hard-coding one rule, while asking what additional evidence and rule configuration derived evaluation requires. (`rfcs/0011-judgment-currency-anchor.md`, Specification §2, Unresolved #3/#10/#11; `rfcs/reviews/0011-round-3.md`, RA-1–RA-3 dispositions.)

6. RB-6 — MAJOR — Section: Unresolved #11. The claimed two-sided “fact” that an artifact was “created against” a head contradicts the later admission that the artifact proves only what its author claims to have relied on. Calling the citation “precisely” what a transition rule needs and saying it gives fail-fast also overclaims: immediate-retirement rules need no historical citation, grace/grandfathering rules need ordering the citation lacks, and mint-time refusal requires a separately chosen transition and accepted-head/freshness policy. Study 014 was reachable and supports only the narrower conclusion that an attacker-controlled internal field cannot establish currency. Fix: say the signed artifact cites a head where `v1` was current, call that one possible input, and make rejection conditional on separately specified policy. (`rfcs/0011-judgment-currency-anchor.md`, Specification §§2–2a, Unresolved #3/#10/#11; [Study 014 `PREREGISTRATION.md` §4c](https://github.com/Judgment-Pack/judgment-pack-evaluator-experiments/blob/main/studies/014-openworkproof-binding/PREREGISTRATION.md#4c-analytic-limitations-not-empirical-rows); `rfcs/reviews/0011-round-1.md`, R-7 disposition.)

7. RB-7 — MINOR — Section: Unresolved #11, split-view limit. A registry head binds one canonical prefix, so split views do not make that cited head ambiguous; they prevent an isolated verifier from establishing that the named view is globally unique or canonical, or from discovering sibling views. Fix: use #9’s exact bound instead of “only as unambiguous as the history it names.” (`rfcs/0011-judgment-currency-anchor.md`, Specification §1 and Unresolved #9/#11; `rfcs/reviews/0011-round-3.md`, RA-3 disposition.)

8. RB-8 — MINOR — Section: Problem and Unresolved #11, public-thread attribution. Problem still calls the remark “independent” and “not a citable public artifact,” while #11 calls it public and claims its citation establishes “by whom.” The supplied link is the thread root rather than an exact comment and the RFC supplies no commenter handle, date, or observable wording; the DEV page could not be verified through the available web access, so the attribution cannot be confirmed. Fix: reconcile Problem, remove the independence claim, and cite the exact dated comment permalink with only the verified public handle and a narrow paraphrase—or remove the attribution. (`rfcs/0011-judgment-currency-anchor.md`, Problem and Unresolved #11; `rfcs/reviews/0011-round-1.md`, R-11 disposition; [cited DEV thread](https://dev.to/kikashy/the-receipt-was-valid-the-policy-was-retired-164a).)

9. RB-9 — MINOR — Section: Security and privacy / Unresolved #11. Existing disclosure analysis covers lifecycle history visible to snapshot readers, but #11 would place a stable head identifier in every signed decision and execution artifact, potentially exposing an actor’s observed view to a wider audience, correlating artifacts that share a head, and retaining that linkage beyond registry access controls. Fix: add the citation’s audience, retention, correlation, and head-resolution risks to Security or #11. (`rfcs/0011-judgment-currency-anchor.md`, Security “Metadata disclosure” and Unresolved #11; `rfcs/0000-rfc-process.md`, Required sections item 7.)

mergeable after listed fixes

## Maintainer dispositions

All nine findings **accepted**. Three of them (RB-2, RB-4, RB-5) are the same failure the first
amendment's review named — answers asserted inside open questions — recurring in new prose, which
is the strongest argument available for why this record exists.

| # | Sev | Disposition |
| --- | --- | --- |
| RB-1 | MAJOR | **Accepted.** §2a's separation made two pieces of pre-existing prose overclaim: the Summary called a decision under a retired version "simply out of date", and Security said such a retirement "is still caught". Under §2a's own examples only non-membership is universal — usability is exactly what is unresolved. Summary now says the version "is no longer in the series' supported set" and defers continued reliance to §2a; Security now says "reported as not current at it". |
| RB-2 | MAJOR | **Accepted.** "Belongs to the relying organization", "none is more correct", and "whoever bears the risk" assigned governance and implementation ownership this record has no basis to assign — a transition rule may be constrained by contract, regulator, or issuing authority, and consumer software may apply a configured rule while still reporting raw membership. §2a is retitled "Membership does not determine continued reliance" and now states only what this registry and §2's primitive do **not** determine, leaving the rule's source, binding force, placement, and evaluation open — with the explicit allowance that a verifier may apply a configured rule provided the membership result it reports is not replaced. |
| RB-3 | MINOR | **Accepted.** "The format carries no expiry semantics" was facially broader than intended, since §1's `retire` is exactly an expiry of a version's membership. Now scoped to **decision-level** expiry/grace/grandfathering, with the distinction stated: `retire` moves a version out of the supported set and says nothing about decisions already made under it. |
| RB-4 | MAJOR | **Accepted.** The "non-binding hint" branch was not genuinely open under a categorical §2a, and the question argued against the option inside itself ("the failure mode this separation exists to prevent") while assuming the currency signer may recommend transition policy despite #1 distinguishing publisher from neutral-log models. §2a's prohibition is narrowed to what the format and primitive *determine*; #10 now asks neutrally who would issue, sign, and label optional advice, notes that the signer models plausibly differ here, and poses "what keeps optional advice optional in practice" as a question rather than an objection. |
| RB-5 | MAJOR | **Accepted.** The closing "asymmetry" sentence was a categorical answer and unsound at its breadth: §2 already calls membership a verdict, membership alone cannot serve window or run-to-expiry rules (they need #3's absent ordering), and a parameterized verifier can expose raw membership *and* compute separately labeled derived results. Replaced with a conditional statement — a membership-only report avoids hard-coding one rule — and the question of what additional evidence and configuration a derived usability result requires. |
| RB-6 | MAJOR | **Accepted.** #11 claimed an artifact was "created against" a head while later admitting it proves only claimed reliance; called the citation "precisely" what a transition rule needs when a stop-at-retirement rule needs no citation and window rules need ordering it lacks; and stated fail-fast as a property of the citation rather than of a separately chosen minting policy. Now: the artifact **cites a head at which the version was in the supported set**, that is *one possible input*, and mint-time refusal is explicitly a separately chosen policy with its own accepted-head and freshness rules. The Study 014 reference is narrowed to the point §4c actually registers. |
| RB-7 | MINOR | **Accepted.** A head binds one prefix unambiguously; the real bound is that an isolated offline verifier cannot establish the named view is globally unique or discover a sibling. #11 now states #9's bound in those terms instead of "only as unambiguous as the history it names". |
| RB-8 | MINOR | **Accepted.** Two attribution defects. The Problem section still called the earlier receipt-protocol remark "independent" — a residual the round-1 R-11 disposition had already ruled out — and that word is now removed. And #11 claimed the citation established "by whom" while naming no one: the reader's public handle (`@circuit`) is now given, and the claim narrowed to "the construction was raised publicly, and nothing about its merit". The link remains the thread root because no comment permalink is held; the record says what it is. |
| RB-9 | MINOR | **Accepted.** A genuinely new disclosure surface the existing metadata analysis does not cover: a stable head identifier embedded in every signed decision and execution artifact reveals which view of a series an actor observed, correlates artifacts sharing a head, and carries that linkage to every audience the artifacts reach, outliving access control on the registry. Added to #11's open list (RFC 0000 item 7). |

Spec suite green (64 tests) after the revision.
